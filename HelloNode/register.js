'use strict';

class register {
    constructor(pRestClient, pbucketManager) {
        this.restClient = pRestClient;
        this.sensor;
        this.bucketManager = pbucketManager;
    }

    registerNewEntry(nodeId) {
        
    }
}